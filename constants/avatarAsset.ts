import type { ImageSourcePropType } from 'react-native';

/** Bundled placeholder when the user has not set a profile photo */
export const defaultAvatarSource: ImageSourcePropType = require('../assets/images/default-avatar.png');
